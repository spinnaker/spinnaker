apiVersion: extensions/v1beta1
kind: replicaSet
metadata:
  name: my-name
  namespace: my-namespace
spec:
  selector:
    matchLabels:
      selector-key: selector-value
  template:
    metadata:
      labels:
        selector-key: selector-value
