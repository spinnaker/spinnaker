dependencies {
  // compile spinnaker.dependency('lombok')
  compile "org.projectlombok:lombok:$lombokVersion"

  // Rserve
  compile group: 'org.rosuda.REngine', name: 'Rserve', version: '1.8.1'
}

test {
  useJUnit {
    excludeCategories 'com.netflix.kayenta.r.CIIgnoreTest'
  }
}
