apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yml
  - service.yml
labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: deck
configMapGenerator:
  - behavior: create
    name: deck
    files:
      - files/settings.js
