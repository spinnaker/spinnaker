apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yml
  - statefulset.yml
  - service.yml
labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: redis
