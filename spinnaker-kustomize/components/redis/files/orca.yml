redis:
  connection: redis://redis:6379

keiko:
  queue:
    sql:
      enabled: false
    redis:
      enabled: true

queue:
  zombieCheck:
    enabled: true
  pendingExecutionService:
    # sql:
      # enabled: false
    redis:
      enabled: true
