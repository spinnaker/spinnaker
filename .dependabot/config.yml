version: 1
update_configs:
  - package_manager: 'javascript'
    directory: '/'
    update_schedule: 'live'
    allowed_updates:
      - match:
          update_type: 'security'
      - match:
          dependency_name: '@spinnaker/kayenta'
      - match:
          dependency_name: '@spinnaker/styleguide'
