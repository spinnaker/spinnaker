kayenta:
  prometheus:
    enabled: true
    health:
      enabled: true
      initial-delay: PT1M
    accounts:
      - name: prometheus-account
        endpoint:
          baseUrl: http://localhost:${embedded.prometheus.port}
        supportedTypes:
          - METRICS_STORE
