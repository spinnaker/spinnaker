kayenta:
  prometheus:
    enabled: true
    health:
      enabled: true
      initial-delay: PT15S
    accounts:
      - name: prometheus-account
        endpoint:
          baseUrl: http://localhost:${embedded.prometheus.port}
        supportedTypes:
          - METRICS_STORE
